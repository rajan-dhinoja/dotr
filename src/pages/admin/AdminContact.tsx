import { PageTypeAdmin } from './PageTypeAdmin';

export default function AdminContact() {
  return (
    <PageTypeAdmin
      pageType="contact"
      typeLabel="Contact"
      description="Manage the Contact page and see how it appears in your navigation."
    />
  );
}
